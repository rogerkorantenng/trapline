import { GetAppBySlugRequest, FullAppInfo, } from '../../../../../devvit/dev_portal/app/app.js';
export class AppServiceClientJSON {
    constructor(rpc) {
        this.rpc = rpc;
        this.GetBySlug.bind(this);
    }
    GetBySlug(request) {
        const data = GetAppBySlugRequest.toJSON(request);
        const promise = this.rpc.request('devvit.dev_portal.v1.AppService', 'GetBySlug', 'application/json', data);
        return promise.then((data) => FullAppInfo.fromJSON(data));
    }
}
export class AppServiceClientProtobuf {
    constructor(rpc) {
        this.rpc = rpc;
        this.GetBySlug.bind(this);
    }
    GetBySlug(request) {
        const data = GetAppBySlugRequest.encode(request).finish();
        const promise = this.rpc.request('devvit.dev_portal.v1.AppService', 'GetBySlug', 'application/protobuf', data);
        return promise.then((data) => FullAppInfo.decode(data));
    }
}
